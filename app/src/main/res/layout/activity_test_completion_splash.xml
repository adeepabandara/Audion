<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/bg_pastel_gradient"
    android:padding="32dp">

    <!-- Animated Success Icon -->
    <ImageView
        android:id="@+id/completionIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/ic_check_circle"
        android:layout_marginBottom="32dp"
        android:tint="@color/success_green"
        android:contentDescription="Test completed successfully" />

    <!-- Completion Message -->
    <TextView
        android:id="@+id/completionText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="All tests are completed!"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="@color/primary_dark"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/subtitleText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generating your results..."
        android:textSize="18sp"
        android:textColor="#757575"
        android:gravity="center"
        android:layout_marginBottom="48dp" />

    <!-- Progress Indicator -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progressIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminate="true"
        app:indicatorColor="@color/primary"
        app:trackThickness="4dp"
        app:indicatorSize="48dp" />

</LinearLayout>
